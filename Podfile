# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RealmTutorial' do
pod 'RealmSwift', '~> 10.1.4', :modular_headers => true
pod 'Realm', '~> 10.1.4', :modular_headers => true

end
